综合比较了Adaboost\GBDT\SVM\RF算法后，发现RF算法是效果最好的

其中模型生成.py是用来生成模型paderborn.model文件的。

归一化.py和去异常点.py也都是实验中用到的代码，然而处理后效果不太好，在测试集上的结果正确率为92%，而不使用却能达到98%，故选择放弃这两个处理。

特征处理_门限.py也是如此，处理过后的效果明显不如不进行特征删选，故不采用该方法进行特征处理。

贡献度2.py和贡献度3.py是用来删除特征的。